package communicationsSystem.model;

// not sure if we discussed whether or not to use this, but I'm adding it here just in case
// and so that I can use it in the User.java class

public enum UserType {
	USER, ADMIN;
}
